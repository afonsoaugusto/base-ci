version: 2.1

orbs:
  clair_scanner: ovotech/clair-scanner@1

jobs:
  build:
    docker:
      - image: afonsoaugusto/base-ci:latest
    # machine: true
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: build
          command: |
            make build
      # - clair_scanner/scan:
      #     image: debian:stretch            
      - run:
          name: publish
          command: |
            make publish

workflows:
  workflow:
    jobs:
      - build:
          context: CI